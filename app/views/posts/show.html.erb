<h2><%= @post.title %></h2>
<%= link_to 'Edit', edit_post_path(@post)%>
|
<%= link_to 'Delete', post_path(@post), method: :delete, data:{confirm: 'Are You realy want to delete this post?'}%>

<p><%= @post.body%></p>